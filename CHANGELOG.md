## 0.0.1

* Initial release.
* Generic implementation of resource states and UI builders for paginated and standard resources.
* Support for standard `ResourceBuilder` and `PaginatedResourceBuilder`.
* Global configuration support via `ResourceConfig`.
